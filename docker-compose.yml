version: '3.1'

services:
  db:
    image: postgres:16.2
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ftc
      POSTGRES_PASSWORD: ftc
      POSTGRES_DB: ftc